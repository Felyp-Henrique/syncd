{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Daemon",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/daemon/main.go"
        }
    ]
}